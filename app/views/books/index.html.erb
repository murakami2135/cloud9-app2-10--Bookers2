<% if @book.errors.any? %>
<div id='error_explanation'>
  <h3><%= @book.errors.count %>error prohibited this obj from being saved:<% end %></h3>
    <ul>
        <% @book.errors.full_messages.each do |message| %>
        <li><%= message %><% end %></li>
    </ul>
<div class="container px-5 px-sm-0">
  <div class="row">
<%= render 'users/list', user:@user, bookers:@bookers %>

<div class="col-md-8 offset-md-1">
  <h2>Books</h2>
  <table class="table table-hover table-inverse">
    <thead>
      <tr>
        <th></th>
        <th>Title</th>
        <th>Opnion</th>
        <th colspan="3"></th>
      </tr>
    </thead>
    <tbody>
      <% @books.each do |books| %>
      <tr>
        <td>
          <%= link_to user_path(books.user) do %>
          <%= attachment_image_tag books.user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg" %>
          <% end %>
          </td>
        <td><%= link_to books.title, "/books/#{books.id}" %></td>
        <td><%= books.body %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
</div>
</div>
</div>
</div>